apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}-configmap
  namespace: {{ .Release.Namespace }}
data:
  AZURE_OPENAI_API_KEY: "dummy-api-key"
  AZURE_OPENAI_ENDPOINT: "https://dummy-resource-name.openai.azure.com"
  AZURE_OPENAI_DEPLOYMENT_NAME: "dummy-deployment-name"
  AZURE_OPENAI_API_VERSION: "dummy-api-version"
